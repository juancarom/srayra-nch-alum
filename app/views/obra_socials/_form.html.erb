<%= form_for(@obra_social) do |f| %>
  <% if @obra_social.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@obra_social.errors.count, "error") %> prohibited this obra_social from being saved:</h2>

      <ul>
      <% @obra_social.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
